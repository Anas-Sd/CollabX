export const metadata = {
  title: 'Login',
  description: 'Sign in to CollabX — your real-time collaborative IDE. Access your workspaces, code with teammates, and execute code live in the browser.',
  alternates: { canonical: 'https://collabx.live/login' },
};

export default function LoginLayout({ children }) {
  return children;
}
